#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - Prints the alphabet in lowercase
 *
 *Return: 0 if success
 **/
int main(void)
{
for (ch = ‘a’; ch <= ‘z’; ch++)
putchar(ch);
putchar (‘\n’); 
return (0);
}
